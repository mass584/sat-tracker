# AGENTS.md

このリポジトリで作業するコーディングエージェント向けの指示です。
利用者向けの説明は [README.md](README.md) にあります。

## プロジェクトの性質

肉眼で見える人工衛星を探すブラウザアプリ。**ビルド工程なし・npm依存なし・バックエンドなし**の
静的ファイルだけで完結させることが設計の前提です。この制約は意図的なものなので、
バンドラやフレームワークを導入しないでください。外部ライブラリが必要な場合は
`vendor/` に実ファイルを置きます（CDN参照はオフラインで壊れるので使いません）。

## 実行と確認

```sh
python3 -m http.server 8123          # → http://localhost:8123/
```

**テストフレームワークはありません。** 変更後は必ず次の2段階で確認してください。

```sh
node --check app.js && node --check clips.js && node --check ar.js
```

そのうえでブラウザで実際に動かします。天文計算が絡む変更では、見た目が正しそうでも
値が間違っていることが頻繁にあるので、**必ず数値で検算してください**。

- 位置：ISSを選び、ⓘダイアログの「実測との照合」が数km以内であること
  （`https://api.wheretheiss.at/v1/satellites/25544` との比較を30秒ごとに自動実行しています）
- 通過予報：軌道傾斜角より高緯度の地点からは見えない、太陽同期軌道は薄明時に集中する、など
  物理的な整合性をスポットチェックする
- 幾何計算：合成した入力を関数に直接与えて期待値と突き合わせる
  （例：ARの投影は「北・水平の衛星が画面中央に来るか」で検証できます）

## 構成

スクリプトは**非モジュール**で、グローバルを共有しています。読み込み順は
`vendor/satellite.min.js` → `world-land.js` → `app.js` → `clips.js` → `ar.js`。

| ファイル | 責務 |
| --- | --- |
| `app.js` | 軌道計算・等級・地図描画・時刻UI・タイミング計算。他ファイルが使う共通関数もここ |
| `clips.js` | 保存したタイミングの永続化と一覧描画 |
| `ar.js` | ARビュー（端末姿勢 → 画面座標の投影、方位盤フォールバック） |
| `world-land.js` | Natural Earth 110m land を2桁に丸めた座標配列（`window.WORLD_LAND`） |

`app.js` の初期化は `DOMContentLoaded` で走ります。`clips.js` / `ar.js` が定義する
`initClips()` `initAr()` `openAr()` を参照するため、**即時実行にすると未定義エラーになります**。

### localStorage のキー

| キー | 内容 |
| --- | --- |
| `sat-tle-cache-v2` | TLE一式（6時間） |
| `sat-selected-v1` | 選択中の衛星のカタログ番号 |
| `sat-observer-v1` | 観測地点 |
| `sat-clips-v1` | 保存したタイミング |
| `sat-calendar-v1` | 追加先のカレンダー（`google` / `apple`。未設定なら初回に選ばせる） |

保存データの構造を変えるときはキーのバージョンを上げるか、**古い形を読めるフォールバックを
必ず用意してください**（利用者のデータを黙って壊さないこと）。

## 触るときに壊しやすいところ

過去に実際に踏んだものです。

### 地図の投影

`px(lon)` は**折り返さない連続値**を返します。地図は `MAP_REPEAT = [-360, 0, 360]` の
3枚重ねで描いており、これで日付変更線をまたぐ軌跡や横スクロールの継ぎ目を消しています。
`px()` に剰余を入れると3枚が重なって破綻します。

点で置くもの（衛星・観測地点・太陽直下点・ラベル）だけが `pxw(lon)` を使い、画面内に折り返します。
横スクロール量は `panLon`（度）で、これも剰余で扱います。

### ダイアログとARの重なり

`<dialog>` はブラウザの top layer に出るため、通常の `z-index` では上に描けません。
ARビューを開く前に `clipsDialog.close()` が必要です。

`showModal()` は開いている `<dialog>` に対して呼ぶと例外になります。カレンダーの予定から
`#clip=...` で戻ってきたときは、すでに開いている場合があるので `dlg.open` を見てから呼びます。

### 端末姿勢（ar.js）

- `deviceorientation` の `alpha` は端末によって真北基準ではありません。iOSは
  `webkitCompassHeading`、Androidは `deviceorientationabsolute` を優先し、
  どちらも無ければ手動較正にフォールバックします
- 較正には `deviceYaw()`（画面右方向＝端末x軸から求める）を使います。
  カメラ軸から求めると、空にかざした姿勢で方位が数学的に不安定になり、
  手首のひねり10°で方位が45°振れます
- `beta = 90 + 仰角` です（`90 − 仰角` ではありません）。合成データで検証する際に間違えやすい点です

### 非同期の待ち

ARビューはカメラと方位センサーの許可を**待たずに**描画を開始します。
`await getUserMedia()` してから描画を始めると、許可ダイアログが出ている間
画面が真っ暗のまま止まります。

タイミング計算は `tleReady` を待ちます。TLE取得前にボタンを押されると
「0件」と表示されてしまうためです。

## 外部データについて

- **Open Notify（open-notify.org）は使わないでください。** 2024年頃で更新が停止しており、
  `iss-now.json` が返す現在位置が実際と約16,000kmずれています（2026-07-31 に確認）。
  座標は滑らかに動くので一見正常に見えますが、古い軌道要素を伝播し続けているだけです。
  `iss-pass.json` は404で廃止済みです
- CelesTrak と wheretheiss.at はどちらも CORS 許可済みで、ブラウザから直接読めます
- CelesTrak の `GROUP=visual` は約157個を返しますが、**選択肢に出すのは
  `FEATURED_ORDER` の8件だけ**です（標準等級が分かっているもの）。
  残りはカタログに保持し、保存済みデータの名前解決に使います

## 公開リポジトリとしての制約

このリポジトリは公開されています。**コミットする内容は全世界から読まれる前提**で扱ってください。

### 絶対にコミットしないもの

- APIキー・アクセストークン・パスワード・VAPID鍵などの認証情報。
  この構成は静的ファイルだけで動くため、**そもそも秘密情報を必要とする設計にしないでください**。
  鍵が要る外部サービスを使いたくなったら、それは設計を見直す合図です
- 個人を特定できる情報。**利用者の自宅・職場などの実座標**、氏名、メールアドレス、
  端末やアカウントの識別子
- ローカル環境に依存するパス（`/Users/<名前>/...`、`~/.asdf/...` など）。
  ドキュメントのコマンド例は、リポジトリのルートからの相対で書きます
- 内部ホスト名・プライベートIP（`192.168.x.x` など）・トンネルの一時URL
  （`*.trycloudflare.com`、`*.ngrok.io`）。これらは検証中にログや手順書へ紛れ込みがちです
- スクリーンショットや録画。画面に位置情報・ブックマーク・他アプリの通知が写り込みます

### 設計上の原則

- **利用者のデータはブラウザの外へ出しません。** 観測地点と保存したタイミングは
  localStorage にのみ置き、どこにも送信しません。この性質を壊す変更（分析ツールの導入、
  位置情報を外部へ送るAPI連携など）は行わないでください。
  **例外は保存した記録の雲量表示（Open-Meteo）だけです**（利用者の明示的な依頼で追加）。
  座標は localStorage に持ち回さず一覧を開くたびに取り直し、README に明記しています。
  これ以上通信先を増やす変更は同じ重さで扱い、実装前に利用者へ確認してください
- 外部への通信先は CelesTrak・wheretheiss.at・Open-Meteo（保存した記録の雲量取得のみ）です。
  **通信先を増やす場合は、何がどこへ送られるかを README に明記してください**。
  特に観測地点は個人の居場所そのものです
- 都市プリセットのような公開されている地点の座標は問題ありません。
  問題になるのは「利用者が実際にいる場所」です

### コミット前の確認

```sh
git diff --cached | grep -niE "api[_-]?key|secret|token|password|bearer|/Users/|192\.168\.|trycloudflare|ngrok"
```

何かヒットしたら、コミットせずに内容を確認してください。

## コードの書き方

- **UI文言・コメントとも日本語**です。用語は「タイミング」に統一しています（「パス」は使いません）
- コメントは**なぜそうしているか**を書きます。何をしているかはコードから読めます
- 定数はファイル冒頭にまとめ、意味を1行で添えます（`PAST_BUFFER_MS`、`SKY_LIMITS` など）
- 概算・近似を使った箇所は、精度の限界をコメントとⓘダイアログの両方に書きます。
  利用者が数字をどこまで信じてよいか判断できることを優先してください
